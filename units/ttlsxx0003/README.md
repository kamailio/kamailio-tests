# TLS domain matching. tls_connection_match_domain=yes #

Summary: check TLS connections matching by TLS domain when tls_connection_match_domain=yes is set

Following tests are done:

  * connection is reused for the same TLS-domain
  * new connection is created for the different TLS domain
