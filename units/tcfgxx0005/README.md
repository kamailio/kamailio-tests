# Default Config File - Sipexer Two-User Call Test #

Summary: default config file - register two users and call with sipexer

Following tests are done:

  * start Kamailio with the default config file
  * use `sipexer --call-user` to register two users, establish a call between
    them, answer the call and hang up
