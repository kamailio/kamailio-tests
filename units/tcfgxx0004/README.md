# Default Config File - Sipexer Self Call Test #

Summary: default config file - register and call self with sipexer

Following tests are done:

  * start Kamailio with the default config file
  * use `sipexer --call-self` to register a user, call it, answer the call and hang up
